server:
  port: 8050
#spring:
#  profiles:
#    active: dev  # 推荐用命名空间来配置，不建议这种
# 在配置中心中，可以通过profile进行设置
# 只有默认的配置文件（和服务名相同的dataid的配置文件）才能结合profile使用，
# 对应dataid：${spring.application.name}-${profile}.${file-extension:properties}
# profile的后缀必须根据默认配置文件的格式来
